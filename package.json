{
  "name": "food_delivery",
  "private": "true",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev:api": "pnpm --filter api run start:dev",
    "dev:mobile": "pnpm --filter mobile run start",
    "dev": "concurrently \"pnpm dev:api\" \"pnpm dev:mobile\"",
    "test": "pnpm -r test",
    "typecheck": "pnpm --filter api exec tsc --noEmit -p tsconfig.json && pnpm --filter mobile typecheck",
    "lint": "pnpm --filter api run lint:check"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.28.2",
  "devDependencies": {
    "concurrently": "^10.0.3",
    "typescript": "^7.0.2"
  }
}
